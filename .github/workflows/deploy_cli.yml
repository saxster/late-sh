name: deploy_cli

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to deploy, for example v0.27.11-cli"
        required: true
        type: string
      environment:
        description: "Deployment environment"
        required: true
        default: production
        type: choice
        options:
          - production
          - staging

concurrency:
  group: ${{ ((github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-cli')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-cli'))) && format('deploy-cli-{0}', github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production')) || format('{0}-{1}', github.workflow, github.run_id) }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: ci
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-cli')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-cli')) }}
    uses: ./.github/workflows/ci.yml
    with:
      package: late-cli
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  build_cli:
    name: build_late_cli_${{ matrix.target }}
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-cli')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-cli')) }}
    needs: ci
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux desktop targets also ship the standalone late-webview
          # helper: late itself must not link WebKitGTK, so a host without
          # the webview libraries can still run the CLI.
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary_name: late
            webview_helper: true
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            binary_name: late
            webview_helper: true
          - runner: ubuntu-latest
            target: x86_64-linux-android
            binary_name: late
            android: true
          - runner: ubuntu-latest
            target: aarch64-linux-android
            binary_name: late
            android: true
          - runner: macos-15-intel
            target: x86_64-apple-darwin
            binary_name: late
          - runner: macos-15
            target: aarch64-apple-darwin
            binary_name: late
          - runner: windows-2025
            target: x86_64-pc-windows-msvc
            binary_name: late.exe
    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

      - name: restore_rust_cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: cli-release-${{ matrix.target }}
          cache-targets: false

      - name: install_linux_build_dependencies
        if: runner.os == 'Linux' && matrix.android != true
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev libwebkit2gtk-4.1-dev pkg-config mold

      - name: set_up_android_sdk
        if: matrix.android == true
        uses: android-actions/setup-android@v4.0.1

      - name: install_android_ndk
        if: matrix.android == true
        env:
          ANDROID_NDK_VERSION: 27.2.12479018
        run: sdkmanager --install "ndk;${ANDROID_NDK_VERSION}"

      - name: configure_android_toolchain
        if: matrix.android == true
        env:
          ANDROID_NDK_VERSION: 27.2.12479018
        run: |
          NDK_HOME="$ANDROID_HOME/ndk/${ANDROID_NDK_VERSION}"
          TOOLCHAIN="$NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64"
          case "${{ matrix.target }}" in
            aarch64-linux-android)
              linker="$TOOLCHAIN/bin/aarch64-linux-android26-clang"
              echo "CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER=$linker" >> "$GITHUB_ENV"
              echo "CC_aarch64_linux_android=$linker" >> "$GITHUB_ENV"
              echo "AR_aarch64_linux_android=$TOOLCHAIN/bin/llvm-ar" >> "$GITHUB_ENV"
              echo "RANLIB_aarch64_linux_android=$TOOLCHAIN/bin/llvm-ranlib" >> "$GITHUB_ENV"
              ln -sf "$TOOLCHAIN/bin/llvm-ranlib" "$TOOLCHAIN/bin/aarch64-linux-android-ranlib"
              ln -sf "$TOOLCHAIN/bin/llvm-ar" "$TOOLCHAIN/bin/aarch64-linux-android-ar"
              ;;
            x86_64-linux-android)
              linker="$TOOLCHAIN/bin/x86_64-linux-android26-clang"
              echo "CARGO_TARGET_X86_64_LINUX_ANDROID_LINKER=$linker" >> "$GITHUB_ENV"
              echo "CC_x86_64_linux_android=$linker" >> "$GITHUB_ENV"
              echo "AR_x86_64_linux_android=$TOOLCHAIN/bin/llvm-ar" >> "$GITHUB_ENV"
              echo "RANLIB_x86_64_linux_android=$TOOLCHAIN/bin/llvm-ranlib" >> "$GITHUB_ENV"
              ln -sf "$TOOLCHAIN/bin/llvm-ranlib" "$TOOLCHAIN/bin/x86_64-linux-android-ranlib"
              ln -sf "$TOOLCHAIN/bin/llvm-ar" "$TOOLCHAIN/bin/x86_64-linux-android-ar"
              ;;
          esac
          echo "$TOOLCHAIN/bin" >> "$GITHUB_PATH"

      - name: configure_windows_msvc_runtime
        if: matrix.target == 'x86_64-pc-windows-msvc'
        shell: pwsh
        run: |
          @(
            'CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_RUSTFLAGS=-C target-feature=+crt-static'
            'CFLAGS_x86_64_pc_windows_msvc=/MT'
            'CXXFLAGS_x86_64_pc_windows_msvc=/MT'
          ) | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: install_rust_target
        run: rustup target add ${{ matrix.target }}

      - name: stamp_cli_version
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            tag="${{ inputs.release_tag }}"
          else
            tag="${{ github.event.release.tag_name }}"
          fi
          test -n "$tag"
          echo "stamping CLI version: ${tag}"
          # build.rs reads this and embeds it as the binary version, matching
          # the VERSION file published by publish_cli for the same tag.
          echo "LATE_CLI_VERSION=${tag}" >> "$GITHUB_ENV"

      - name: cargo_build_cli
        if: matrix.android != true
        run: cargo build --locked --release -p late-cli --bin late --target ${{ matrix.target }}

      - name: cargo_build_cli_android
        if: matrix.android == true
        run: cargo build --locked --release -p late-cli --bin late --target ${{ matrix.target }}

      - name: cargo_build_webview_helper
        if: matrix.webview_helper == true
        run: cargo build --locked --release -p late-webview --bin late-webview --target ${{ matrix.target }}

      - name: stage_cli_artifact
        shell: bash
        run: |
          mkdir -p dist/${{ matrix.target }}
          cp "target/${{ matrix.target }}/release/${{ matrix.binary_name }}" "dist/${{ matrix.target }}/${{ matrix.binary_name }}"
          if [[ "${{ matrix.webview_helper }}" == "true" ]]; then
            cp "target/${{ matrix.target }}/release/late-webview" "dist/${{ matrix.target }}/late-webview"
          fi

      - name: upload_cli_artifact
        uses: actions/upload-artifact@v6
        with:
          name: late-${{ matrix.target }}
          path: dist/${{ matrix.target }}/
          if-no-files-found: error

  publish_cli:
    name: publish_late_cli
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-cli')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-cli')) }}
    needs: build_cli
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease == true && 'staging' || 'production') }}
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.S3_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: auto
      DIST_BUCKET: ${{ vars.BUCKET || 'late-sh-r' }}
      S3_ENDPOINT: ${{ vars.S3_ENDPOINT }}
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.event.release.tag_name }}
      IS_PRERELEASE: ${{ (github.event_name == 'workflow_dispatch' && inputs.environment == 'staging') || (github.event_name != 'workflow_dispatch' && github.event.release.prerelease) }}
    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

      - name: download_cli_artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: late-*
          path: dist

      - name: validate_publish_configuration
        run: |
          test -n "$AWS_ACCESS_KEY_ID"
          test -n "$AWS_SECRET_ACCESS_KEY"
          test -n "$DIST_BUCKET"
          test -n "$S3_ENDPOINT"

      - name: prepare_distribution_layout
        run: |
          chmod 755 scripts/install.sh
          mkdir -p "publish/releases/${RELEASE_TAG}"

          for artifact_dir in dist/late-*; do
            target="${artifact_dir#dist/late-}"
            mkdir -p "publish/releases/${RELEASE_TAG}/${target}"
            main_binary_found=0
            for binary_name in late late.exe late-webview; do
              source_file="${artifact_dir}/${binary_name}"
              [[ -f "$source_file" ]] || continue
              if [[ "$binary_name" != "late-webview" ]]; then
                main_binary_found=1
              fi
              cp "$source_file" "publish/releases/${RELEASE_TAG}/${target}/${binary_name}"
            done
            test "$main_binary_found" -eq 1
          done

          cp scripts/install.sh "publish/releases/${RELEASE_TAG}/install.sh"
          cp scripts/install.ps1 "publish/releases/${RELEASE_TAG}/install.ps1"
          printf '%s\n' "${RELEASE_TAG}" > "publish/releases/${RELEASE_TAG}/VERSION"
          (
            cd "publish/releases/${RELEASE_TAG}"
            files=()
            while IFS= read -r -d '' file; do
              files+=("${file#./}")
            done < <(find . -mindepth 2 -maxdepth 2 -type f \( -name 'late' -o -name 'late.exe' -o -name 'late-webview' \) -print0 | LC_ALL=C sort -z)
            sha256sum "${files[@]}" > sha256sums.txt
          )

          if [[ "${IS_PRERELEASE}" != "true" ]]; then
            mkdir -p publish/latest
            for target_dir in "publish/releases/${RELEASE_TAG}"/*; do
              [[ -d "$target_dir" ]] || continue
              cp -R "$target_dir" publish/latest/
            done
            cp scripts/install.sh publish/latest/install.sh
            cp scripts/install.ps1 publish/latest/install.ps1
            cp scripts/install.sh publish/install.sh
            cp scripts/install.ps1 publish/install.ps1
            cp scripts/install.sh publish/index.html
            printf '%s\n' "${RELEASE_TAG}" > publish/latest/VERSION
            printf '%s\n' "${RELEASE_TAG}" > publish/VERSION
            cp "publish/releases/${RELEASE_TAG}/sha256sums.txt" publish/latest/sha256sums.txt
          fi

      - name: upload_release_distribution
        run: |
          aws s3 cp \
            "publish/releases/${RELEASE_TAG}/" \
            "s3://${DIST_BUCKET}/releases/${RELEASE_TAG}/" \
            --recursive \
            --endpoint-url "${S3_ENDPOINT}"

      - name: upload_latest_distribution
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.environment == 'production') || (github.event_name != 'workflow_dispatch' && github.event.release.prerelease == false) }}
        run: |
          aws s3 cp \
            publish/latest/ \
            "s3://${DIST_BUCKET}/latest/" \
            --recursive \
            --endpoint-url "${S3_ENDPOINT}"

          aws s3 cp \
            publish/install.sh \
            "s3://${DIST_BUCKET}/install.sh" \
            --endpoint-url "${S3_ENDPOINT}" \
            --content-type "text/x-shellscript"

          aws s3 cp \
            publish/install.ps1 \
            "s3://${DIST_BUCKET}/install.ps1" \
            --endpoint-url "${S3_ENDPOINT}" \
            --content-type "text/plain"

          aws s3 cp \
            publish/index.html \
            "s3://${DIST_BUCKET}/index.html" \
            --endpoint-url "${S3_ENDPOINT}" \
            --content-type "text/x-shellscript"

          aws s3 cp \
            publish/VERSION \
            "s3://${DIST_BUCKET}/VERSION" \
            --endpoint-url "${S3_ENDPOINT}" \
            --content-type "text/plain"
