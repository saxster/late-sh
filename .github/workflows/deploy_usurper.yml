name: deploy_usurper

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to deploy, for example v0.27.11-usurper"
        required: true
        type: string
      environment:
        description: "Deployment environment"
        required: true
        default: production
        type: choice
        options:
          - production
          - staging

concurrency:
  group: ${{ ((github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-usurper')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-usurper'))) && format('deploy-usurper-{0}', github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production')) || format('{0}-{1}', github.workflow, github.run_id) }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  ci:
    name: ci
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-usurper')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-usurper')) }}
    uses: ./.github/workflows/ci.yml
    with:
      package: late-usurper
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  build_usurper:
    name: build_late_usurper_image
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-usurper')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-usurper')) }}
    needs: ci
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      packages: write
    secrets: inherit
    with:
      registry: ghcr.io
      image_name: late-usurper
      image_tag: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.event.release.tag_name }}
      context: .
      dockerfile: ./Dockerfile
      docker_target: runtime-usurper
      environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  get_images:
    name: get_current_ssh_web_image_tags
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-usurper')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-usurper')) }}
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    outputs:
      ssh_image_tag: ${{ steps.ssh_image.outputs.image_tag }}
      web_image_tag: ${{ steps.web_image.outputs.image_tag }}
      nethack_image_tag: ${{ steps.nethack_image.outputs.image_tag }}
      dopewars_image_tag: ${{ steps.dopewars_image.outputs.image_tag }}
      dcss_image_tag: ${{ steps.dcss_image.outputs.image_tag }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: get_ssh_image_tag
        id: ssh_image
        run: |
          tag=$(kubectl get deployment service-ssh -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

      - name: get_web_image_tag
        id: web_image
        run: |
          tag=$(kubectl get deployment service-web -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

      - name: get_nethack_image_tag
        id: nethack_image
        run: |
          tag=$(kubectl get deployment late-nethack -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

      - name: get_dopewars_image_tag
        id: dopewars_image
        run: |
          tag=$(kubectl get deployment late-dopewars -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

      - name: get_dcss_image_tag
        id: dcss_image
        run: |
          tag=$(kubectl get deployment late-dcss -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

  terraform:
    name: terraform_apply
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-usurper')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-usurper')) }}
    needs: [build_usurper, get_images]
    uses: ./.github/workflows/terraform.yml
    permissions:
      contents: read
    secrets: inherit
    with:
      ssh_image_tag: ${{ needs.get_images.outputs.ssh_image_tag }}
      web_image_tag: ${{ needs.get_images.outputs.web_image_tag }}
      nethack_image_tag: ${{ needs.get_images.outputs.nethack_image_tag }}
      dopewars_image_tag: ${{ needs.get_images.outputs.dopewars_image_tag }}
      dcss_image_tag: ${{ needs.get_images.outputs.dcss_image_tag }}
      usurper_image_tag: ${{ needs.build_usurper.outputs.image_tag }}
      environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  wait_for_usurper_rollout:
    name: wait_for_usurper_rollout
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-usurper')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-usurper')) }}
    needs: terraform
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: wait_for_rollout
        run: kubectl rollout status deployment late-usurper -n default --timeout=180s
