name: deploy_web

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to deploy, for example v0.27.11-web"
        required: true
        type: string
      environment:
        description: "Deployment environment"
        required: true
        default: production
        type: choice
        options:
          - production
          - staging

concurrency:
  group: ${{ ((github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web'))) && format('deploy-web-{0}', github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production')) || format('{0}-{1}', github.workflow, github.run_id) }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  ci:
    name: ci
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    uses: ./.github/workflows/ci.yml
    with:
      package: late-web
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  build_web:
    name: build_late_web_image
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    needs: ci
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      packages: write
    secrets: inherit
    with:
      registry: ghcr.io
      image_name: late-web
      image_tag: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.event.release.tag_name }}
      context: .
      dockerfile: ./Dockerfile
      docker_target: runtime-web
      environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  get_ssh_image:
    name: get_current_ssh_image_tag
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    outputs:
      ssh_image_tag: ${{ steps.ssh_image.outputs.image_tag }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: get_ssh_image_tag
        id: ssh_image
        run: |
          tag=$(kubectl get deployment service-ssh -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

  get_nethack_image:
    name: get_current_nethack_image_tag
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    outputs:
      nethack_image_tag: ${{ steps.nethack_image.outputs.image_tag }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: get_nethack_image_tag
        id: nethack_image
        run: |
          tag=$(kubectl get deployment late-nethack -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

  get_dopewars_image:
    name: get_current_dopewars_image_tag
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    outputs:
      dopewars_image_tag: ${{ steps.dopewars_image.outputs.image_tag }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: get_dopewars_image_tag
        id: dopewars_image
        run: |
          tag=$(kubectl get deployment late-dopewars -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

  get_dcss_image:
    name: get_current_dcss_image_tag
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    outputs:
      dcss_image_tag: ${{ steps.dcss_image.outputs.image_tag }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: get_dcss_image_tag
        id: dcss_image
        run: |
          tag=$(kubectl get deployment late-dcss -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

  get_usurper_image:
    name: get_current_usurper_image_tag
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    outputs:
      usurper_image_tag: ${{ steps.usurper_image.outputs.image_tag }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: get_usurper_image_tag
        id: usurper_image
        run: |
          tag=$(kubectl get deployment late-usurper -n default -o jsonpath='{.spec.template.spec.containers[0].image}')
          echo "image_tag=$tag" >> "$GITHUB_OUTPUT"

  terraform:
    name: terraform_apply
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    needs: [build_web, get_ssh_image, get_nethack_image, get_dopewars_image, get_dcss_image, get_usurper_image]
    uses: ./.github/workflows/terraform.yml
    permissions:
      contents: read
    secrets: inherit
    with:
      ssh_image_tag: ${{ needs.get_ssh_image.outputs.ssh_image_tag }}
      web_image_tag: ${{ needs.build_web.outputs.image_tag }}
      nethack_image_tag: ${{ needs.get_nethack_image.outputs.nethack_image_tag }}
      dopewars_image_tag: ${{ needs.get_dopewars_image.outputs.dopewars_image_tag }}
      dcss_image_tag: ${{ needs.get_dcss_image.outputs.dcss_image_tag }}
      usurper_image_tag: ${{ needs.get_usurper_image.outputs.usurper_image_tag }}
      environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
      source_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref }}

  wait_for_web_rollout:
    name: wait_for_web_rollout
    if: ${{ (github.event_name == 'workflow_dispatch' && endsWith(inputs.release_tag, '-web')) || (github.event_name != 'workflow_dispatch' && endsWith(github.ref, '-web')) }}
    needs: terraform
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || (github.event.release.prerelease && 'staging' || 'production') }}
    steps:
      - name: setup_kubectl
        uses: azure/setup-kubectl@v5

      - name: set_kubernetes_context
        uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: wait_for_rollout
        run: kubectl rollout status deployment service-web -n default --timeout=180s
